/*
  A simple example with lists.
  For demo:
  @input: a list L1
*/
i = 3;
while (i > 0) {
  L1.pushFront(i);
  i = i-1;
}
while (L1.size() > 0) {
  L2.pushBack(L1.topFront());
  L1.popFront();
}

/*
  Example of running command line:
    krun programs/list2.alk -cST="L1 |-> [.Vals] L2 |-> [.Vals]" \
                            -cGST=".Map"
*/