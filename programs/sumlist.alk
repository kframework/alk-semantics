/*
  Another simple example with lists.
  For demo:
  @input: a list L
  @output: the sum s of the elements of L
*/
s = 0;
i = 0;
while (i < L.size()) {
  s = s + L.select(i);
  i = i + 1;
}


/*
  Example of running command line:
    krun programs/sumlist.alk -cST="L |-> [1, 2, 3]" 
*/